language: php

php:
 - 7.1

services:
  - mysql

before_install:
  - composer global require "hirak/prestissimo:^0.3"

install:
  - composer install

before_script:
- mysql -u root -e "CREATE USER 'traves'@'localhost' IDENTIFIED BY 'traves';"
- mysql -u root -e "GRANT ALL ON *.* TO 'traves'@'localhost';"

script: tools/scripts/dummy.sh

notifications:
  email: false
